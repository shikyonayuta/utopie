import { useState } from 'react'
import {render, Box, Text} from 'ink';
import { useTextInput } from 'ink-text-input';
import { useSelectInput } from 'ink-select-input';

function App() {
  const [] = useState(0)

  return (
    <div className="app">
    </div>
  )
}

export default App
